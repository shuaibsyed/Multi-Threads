# Multi-Threads
This project focused on creating a multi-threaded program that emulates the functionality of the UNIX utility "word count" (wc). By leveraging multiple threads, the program efficiently processed and analyzed text files to calculate word, line, and character counts. 
